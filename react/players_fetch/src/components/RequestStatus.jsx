/**
 * Student instructions:
 *
 * Use the given template with props to create a component that displays the
 * status of a request. Instead of using <template>, use JSX.
 *
 * REMEMBER: use the same ids, classes and attributes as in the Vue exercise
 * in the same places to pass the tests.
 *
 * @format
 */

export const RequestStatus = ({ status }) => {
	return (
		<div>
			<h3>Request status</h3>
			TODO: RequestStatus
		</div>
	);
};
